language: python
install:
  - pip install tox 
  - pip install python-coveralls
script:
  - tox
env:
  - TOXENV: py34
after_success:
    coveralls --data_file humantrafficking_tips/.coverage --config_file humantrafficking_tips/.coveragerc
